apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
  - ../../base
  - namespace.yaml

commonLabels:
  environment: dev

patches:
  - path: replica-patch.yaml
  - path: ingress-patch.yaml
